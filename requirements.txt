torch --index-url https://download.pytorch.org/whl/cu121  # Adjust CUDA version if needed (Vast.ai often uses cu121 or cu128)
unsloth[cu121-torch220] @ git+https://github.com/unslothai/unsloth.git  # Unsloth for QLoRA (replace cu121 with your CUDA)
trl>=0.20.0
peft>=0.17.0
transformers>=4.55.0
datasets
bitsandbytes>=0.43.0  # For 4-bit quantization
accelerate
huggingface_hub