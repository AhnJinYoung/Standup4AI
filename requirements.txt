# Core
torch>=2.5.0
transformers>=4.55.0
accelerate>=0.34.2
datasets>=2.21.0
trl>=0.20.0
peft>=0.17.0

# Quant & kernels
triton>=3.4.0

# Optional: 8-bit optimizer for smaller optimizer states
bitsandbytes>=0.43.0

# (Do NOT pin flash-attn here; OSS attention for gpt-oss often prefers 'eager')
