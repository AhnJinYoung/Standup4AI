# === Core HF stack ===
transformers>=4.55.0
trl>=0.20.0
peft>=0.17.0
datasets>=2.20.0
accelerate>=0.33.0
huggingface_hub>=0.24.6
tokenizers>=0.19.1
safetensors>=0.4.4
einops>=0.7.0

# === Utilities / I/O ===
tqdm>=4.66.0
pyarrow>=14.0.2         # parquet 저장/로드
pandas>=2.2.2

# === Optional but handy ===
rich>=13.7.0            # 예쁜 로그
evaluate>=0.4.2         # (선택) 간단 평가 지표
wandb>=0.17.0           # (선택) 실험 로깅

# === 4bit/QLoRA 등 (선택) ===
bitsandbytes>=0.43.1; platform_system != "Darwin"
